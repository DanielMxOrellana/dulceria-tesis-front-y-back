services:
  postgres:
    image: postgres:16-alpine
    container_name: dulceria-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: dulceria
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    container_name: dulceria-backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env
    depends_on:
      - postgres
    restart: unless-stopped

  frontend:
    build:
      context: ./dulceria-tesis
    container_name: dulceria-frontend
    ports:
      - "3000:3000"
    env_file:
      - ./dulceria-tesis/.env
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  pgdata:
